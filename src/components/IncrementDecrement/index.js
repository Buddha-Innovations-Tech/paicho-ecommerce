import React, { useState } from "react";
import { BiPlus, BiMinus } from "react-icons/bi";

const IncrementDecrement = ({ increase }) => {
  const [count, setCount] = useState(0);
  return <></>;
};

export default IncrementDecrement;
